\begin{sanefig}
  \subfigure[][Time in per-crashing instruction phase, per crashing instruction, seconds]{
    \input{eval/alpha/time_per_crashing.tex}
    \label{fig:eval:alpha:time_per_crashing}
  }
  \subfigure[][Time in per-interfering \gls{cfg} phase, per interfering \gls{cfg}, seconds]{
    \input{eval/alpha/time_per_interfering.tex}
    \label{fig:eval:alpha:time_per_interfering}
  }
  {\hfill}\subfigure[][Interfering \glspl{cfg} per crashing instruction]{
    \input{eval/alpha/interfering_per_crashing.tex}
    \label{fig:eval:alpha:interfering_per_crashing}
  }{\hfill}%
  \subfigure[][Proportion of interfering \glspl{cfg} generating
    \glspl{verificationcondition}]{
    \input{eval/alpha/vcs_per_interfering.tex}
    \label{fig:eval:alpha:vcs_per_interfering}
  }{\hfill}%
  \vspace{-8pt}\caption{The effect of the \gls{alpha} parameter.  The violin plots
    show the PDFs of the metrics, with the failure rates above.  Crosses
    give the means of the metrics.  Circles give the observed ratio.
    Bars and grey regions give 90\% bootstrapped confidence
    intervals.  Censoring corrections were applied to the PDFs near the 300
    second timeout.}
  \label{fig:eval:alpha}
\end{sanefig}
